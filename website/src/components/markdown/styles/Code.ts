import styled from "@emotion/styled";

export const Code = styled.code`
    padding: 0.2em;
    margin: 0.1em 0;

    border-radius: 3px;
    background: #ddd;

    font-size: 0.75em;
    line-height: 1.2rem;

    white-space: pre-wrap;
`;
