import { ServerLayout } from "../components/layouts/ServerLayout";
import React from "react";

export const ChannelsIndexPage: React.FC<{ serverId: string }> = () => {
    return <ServerLayout name="Hello" />;
};
